TOSMAKE_PATH=../support/make/

COMPONENT=GlobalTestAppC

#CFLAGS += -DFIX_WAKEUP #HARDCODING OF WAKEUP INTERVALS TO FIX VALUES
CFLAGS += -DCC2420_DEF_RFPOWER=27
CFLAGS += -DCC2420_DEF_CHANNEL=26
CFLAGS += -DACK_LOW_POWER_LISTENING
MSG_SIZE = 94


## DEBUG / TEST (REMOVE WHEN DEPLOYING)
#CFLAGS += -DCOOJA    # USED TO SET THE PROPER PRINTF COMPONENT FOR COOJA
#CFLAGS += -DDUMMY_START  # MAKES THE SINK START WITHOUT SERIAL INPUT (5S FROM BOOT)
#CFLAGS += -DPRINTF	# ENABLES PRINTF FOR DEBUGGING
#FLAGS += -DPRINTF_BUFFER_SIZE=500
#CFLAGS += -I$(TOSDIR)/lib/printf
#BUILD_EXTRA_DEyyPS += PrintfMsg.class PrintfClient.class
## END DEBUG / TEST


#CFLAGS += -DNOISES	#activates the sensing of the noise floor
#CFLAGS += -DPRINTF
#CFLAGS += -DPRINTF_BUFFER_SIZE=2000

PFLAGS += -DCTP_CLEAR
PFLAGS += -DLPL_COEXISTENCE
PFLAGS += -DCTP_REPORT
PFLAGS += -DRADIO_ACTIVATIONS

CFLAGS += -IroutingTest/common \
	  -IroutingTest/common/routingInfo \
	  -IPLR
PFLAGS += -DCTP_RADIO_SETTINGS

ifdef LSLEEP
CFLAGS += -DLOCAL_SLEEP=$(LSLEEP)
endif

ifeq ($(OBJECTIVE),SINK)
CFLAGS += -IroutingTest/sink_node 
CFLAGS += -DSERIAL_FW
#CFLAGS += -DMSG_LOGGER
endif

ifeq ($(OBJECTIVE),WINE_SINK)
CFLAGS += -IroutingTest/sink_node
#CFLAGS += -DALTERNATE_RADIO_FLASH
endif

ifeq ($(OBJECTIVE),NET)
CFLAGS += -IroutingTest/net_node
endif

ifeq ($(OBJECTIVE),BRIDGE_SINK)
CFLAGS += -DROUTING_NODE
CFLAGS += -DSERIAL_FW
CFLAGS += -IroutingTest/bridge_node 
endif

ifeq ($(OBJECTIVE),BRIDGE)
CFLAGS += -DROUTING_NODE
CFLAGS += -IroutingTest/bridge_node 
endif

#CFLAGS += -DPRINTF_SUPPORT
#CFLAGS += -I$(TOSDIR)/lib/printf
#BUILD_EXTRA_DEyyPS += PrintfMsg.class PrintfClient.class
# %.class: %.java 
# 	javac $<

include $(MAKERULES)

